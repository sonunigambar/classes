spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/sonunigambar/configRepo
          default-label: main

server:
  port: 9098